(ns hackerrank.bfs-test
  (:require [clojure.test :refer :all]
            [hackerrank.bfs :refer :all]))

; (deftest a-test
;   (testing "FIXME, I fail."
;     (is (= 0 1))))

(deftest parseInt-test
  (testing "parseInt"
    (is (= (parseInt "1") 1))))

; (deftest get-edge-test
; 	(testing "get-edge"
; 		(is ())))
